//document.getElementById("menu-icon").addEventListener("onclick",toggleMenu());

function toggleMenu() {
	var element = document.getElementById("navigation");
	element.classList.toggle("hide");
	//element.classList.toggle("show");
}
